func hola()
begin
end;

program
    write "Insert a number : ";
	
	for i from
	
    with
        number n;
    do
        read n; # I suggest that this number be a multiple of 360
        with
            number angle = n;
        do
            while angle <= 360 do
                #rotater(n);
                hola();
				write "holo";
                angle = angle + n;
            end;
        end;
    end;
end;
